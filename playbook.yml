  - name: Playbook
    hosts: all
    remote_user: centos
    become: 'yes'
    become_user: root

    # created with:
    # python -c 'import crypt; print(crypt.crypt("it351mu", "$1$SomeSalt$"))'
    # password: $1$SomeSalt$UqddPX3r4kH3UL5jq5/ZI.

    tasks:
      - name: Install Dependencies
        yum:
          name: epel-release
          state: latest

      - name: Install FTP Server
        yum:
          name: proftpd
          state: latest

      - name: Start FTP Server
        ansible.builtin.systemd:
          state: started
          name: proftpd

      - name: Enable FTP Server
        ansible.builtin.systemd:
          name: proftpd
          enabled: yes
      
      - name: Create a user with hashed password
        user:
          name: devsecops
          shell: /bin/bash
          password: it351mu


          