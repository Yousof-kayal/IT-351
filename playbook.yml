  - name: Playbook
    hosts: all
    remote_user: centos
    become: 'yes'
    become_user: root
    
    vars:
    # created with:
    # python -c 'import crypt; print(crypt.crypt("crytography_is_cool"))
    password: $6$6cVseYIL/n/.kmRF$SK5ucdw0mp8MmBBT/HUcC0ozMWALdJKi3ejKKIatqz56.kUvbvKmrs/tFSXcEU/4NjVSqtqXLTsCTzrlalni3/

    tasks:
      - name: Install Dependencies
        yum:
          name: epel-release
          state: latest

      - name: Install FTP Server
        yum:
          name: proftpd
          state: latest

      - name: Start FTP Server
        ansible.builtin.systemd:
          state: started
          name: proftpd

      - name: Enable FTP Server
        ansible.builtin.systemd:
          name: proftpd
          enabled: yes
      
      - name: Create New User
        ansible.builtin.user:
          name: devsecops
          shell: /bin/bash
          password: {{password}}


          